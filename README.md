
  # Mobile Food Donation App UI

  This is a code bundle for Mobile Food Donation App UI. The original project is available at https://www.figma.com/design/7MpKVlYcUblZ2f1an0RKty/Mobile-Food-Donation-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  